 
 ## Start by creating a new SQLite database
#### Open your terminal or command prompt and run:

### sqlite3 DATABASE_NAME.db


#### If DATABASE_NAME.db does not exist, SQLite will create it.

You will enter the SQLite interactive shell (sqlite> prompt).
